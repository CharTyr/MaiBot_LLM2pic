"""draw_picture Tool 元数据。"""

from .image_clients import ImageClientMixin


class DrawPictureToolMetadata(ImageClientMixin):
    """生成一张图片并发送。"""

    tool_description = (
        "生成并发送图片（纯文生图，二次元/动漫风格）。触发场景：用户让你画图/发图/自拍、"
        "要求看你长什么样/在干嘛、说\"画一张...\"\"给我看看...\"。调用时必须把用户明确要求的主体、"
        "角色名、动作和场景完整写进 description，禁止用你自己替换用户指定主体。如果用户发送或引用了图片"
        "并要求基于该图片画图/改图/仿画风，也使用此工具——系统会自动反推参考图的 tag 并融合用户文字。"
        "只有用户明确要求画图/出图/发自拍时才能调用此工具。注意：如果用户发送了图片并要求修改/编辑，"
        "或明确要求写实风格，请使用 edit_picture 工具。"
    )

    tool_detailed_description = """
此工具让你能够从零生成并发送二次元/动漫风格的图片，用于回应群友想要"看到"某些视觉内容的请求。仅用于文生图。

【最高优先级参数规则】
- description 必须忠实复述用户明确要求的主体、角色名、动作、服装状态和场景。
- 如果用户要求画某个具体角色/人物/物品，必须把该主体原样写入 description。
- 只有用户明确要求"你/东雪莲/自拍/你的当前状态"时，selfie_mode 才能为 true。
- 禁止把普通角色请求改写成东雪莲/Azuma Seren/你自己。

【触发条件】仅当群友明确要求你生成/发送以下图片内容时使用：
1. 你当前的状态/环境/正在做的事（自拍、你在哪、你在干嘛）
2. 你拍的照片/摄影作品（发张你拍的照片、看看你的摄影）
3. 你正在吃/喝/用的东西（你在吃什么、给我看看）
4. 你画的画/创作的图（画一张、帮我画个）
5. 某个具体场景/角色/事物的图片（我想看看...的样子）

【典型触发语句示例】
- "自拍/来张自拍/发张照片看看"
- "你现在在哪/在干嘛，发张图看看"
- "画一张.../帮我画个..."
- "我想看看...长什么样"

【禁止触发 - 必须使用 edit_picture 的情况】
- 用户发送了图片并要求修改/编辑/重绘/换风格
- 用户要求生成写实/真实/照片级别的图片

【禁止触发 - 不应生成图片的情况】
- 纯文字聊天、问答、讨论（不涉及"看图"需求）
- 只是提到图片相关词汇但不是要求生成
- 只是 @你、提到你、比较你或询问你的看法
- 讨论或评价已经存在的图片（不要求修改）
- 用户明确表示不需要图片
- 并不是对你提出的看图需求
- 前面聊天记录中你已经发过图片时，禁止再次生成并发送图片
"""
